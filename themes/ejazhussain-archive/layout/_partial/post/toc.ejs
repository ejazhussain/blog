<% if(theme.toc){ 
    var topic = toc(post.content, {
            class: 'post-toc',
            list_number: false
        })
    if(topic) {

    %>
    
        <nav id="anchors" class="aside-anchors is-active is-hidden-mobile">
            <p class="bd-anchors-title">
                On this page
            </p>

            <%- toc(post.content, {
                class: 'post-toc',
                list_number: true
            }) %>
        </nav>
    
    <% }
} %>